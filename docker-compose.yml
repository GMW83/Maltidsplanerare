services:
  maltidsplanerare:
    build:
      context: .
      network: host
    container_name: maltidsplanerare
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 1.1.1.1
