services:
  maltidsplanerare:
    build: .
    container_name: maltidsplanerare
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
